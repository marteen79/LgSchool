<local:NowyViewBase x:Class="LgSchool.View.NowyTytulWplatyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LgSchool.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Content="Numer wpłaty" Margin="0,8,0,8"/>
        <ComboBox 
            Grid.Column="1" 
            Grid.Row="0" 
            Margin="0,8,0,8" 
            ItemsSource="{Binding Path=WplatyComboboxItems}"
            DisplayMemberPath="Value"
            SelectedValuePath="Key"
            SelectedValue="{Binding Path=WplataID, Mode=TwoWay}"/>

        <Label Grid.Column="0" Grid.Row="1" Content="Nazwa kursu" Margin="0,8,0,8"/>
        <ComboBox 
            Grid.Column="1" 
            Grid.Row="1" 
            Margin="0,8,0,8" 
            ItemsSource="{Binding Path=KursyComboboxItems}"
            DisplayMemberPath="Value"
            SelectedValuePath="Key"
            SelectedValue="{Binding Path=KursID, Mode=TwoWay}"/>

        <Label Grid.Column="0" Grid.Row="2" Content="Cena" Margin="0,2,0,2"/>
        <TextBox Grid.Column="1" Grid.Row="2" Margin="0,2,0,2" Text="{Binding Path=Cena, 
            UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Column="0" Grid.Row="3" Content="Liczba godzin" Margin="0,2,0,2"/>
        <TextBox Grid.Column="1" Grid.Row="3" Margin="0,2,0,2" Text="{Binding Path=IloscGodzin, 
            UpdateSourceTrigger=PropertyChanged}"/>

    </Grid>
</local:NowyViewBase>
