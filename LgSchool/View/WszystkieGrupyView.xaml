<local:WszystkieViewBase x:Class="LgSchool.View.WszystkieGrupyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LgSchool.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Numer grupy" Binding="{Binding Numer}"/>
                <DataGridTextColumn Header="Nazwa grupy" Binding="{Binding Nazwa}"/>
                <DataGridTextColumn Header="Opis" Binding="{Binding Opis}"/>
                <DataGridTextColumn Header="Poziom" Binding="{Binding PoziomNazwa}"/>
                <DataGridTextColumn Header="Nazwa kursu" Binding="{Binding KursNazwa}"/>
                
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
